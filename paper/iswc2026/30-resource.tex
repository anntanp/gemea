\section{Resource Description}
\label{sec:resource}

% TODO (~3–4 pages)
% - Overview: GeMeA as app + dataset (Virtuoso triple store + Elasticsearch index)
% - Source data: DDB (Deutsche Digitale Bibliothek), EDM records, 65M objects
% - Pipeline: DDB JSON-LD → rdf2jsonld → mocho (RDA/FRBR normalization) → bulk load
% - Data model: RDA WEMI hierarchy, EDM entities (ProvidedCHO, Aggregation, WebResource,
%   Agent, Place, TimeSpan), mocho alignment
% - Schema diagram (figure)
% - Scale: objects, triples, agents, places, time spans, providers/institutions
% - Named graph strategy (partition by provider)
% - Web application components: search, facets, entity pages, graph viz, map, timeline
% - SPARQL endpoint: access URL, supported query patterns, rate limits
% - VoID / DCAT dataset descriptor
\todo{Resource Description.}
