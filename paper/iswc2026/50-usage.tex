\section{Usage and Reusability}
\label{sec:usage}

% TODO (~1–2 pages)
% - Web UI: search bar, facets (type/place/time/institution), entity pages, graph viz, map, timeline
% - SPARQL endpoint: example queries (keyword search, geo-filter, timespan, agent lookup)
% - Self-hosting: Docker Compose, hardware requirements, lite mode for dev
% - Example SPARQL queries (listings with SPARQL syntax highlighting):
%   1. Find all objects by a given agent (GND URI)
%   2. Objects created in a given place and decade
%   3. Count objects per provider institution
%   4. 1-hop neighborhood of a ProvidedCHO
% - Extension points: adding new providers, enrichment with Wikidata/GND
% - Tutorials / documentation links
\todo{Usage and Reusability.}
