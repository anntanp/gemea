\section{Introduction}
\label{sec:intro}

% TODO (~1 page)
% - Problem: 65M DDB cultural heritage objects rich in metadata but not graph-accessible
% - Gap: no SPARQL endpoint, no linked-data browser, metadata locked in proprietary JSON-LD;
%   existing CH KGs are either small/curated (ArtKB: 307K art objects from Wikidata) or
%   institution-specific — none expose a national cross-domain collection at this scale
%   with a public SPARQL endpoint and a full web UI for non-technical users
% - Contribution: GeMeA — open KG browser (search, facets, entity pages, map, timeline,
%   graph viz, SPARQL + GraphQL endpoints, self-hostable)
% - Pipeline: DDB JSON-LD → rdf2jsonld → mocho (RDA normalization) → triplestore + Elasticsearch → web UI
% - Paper roadmap (1 sentence per section)
\todo{Introduction.}
